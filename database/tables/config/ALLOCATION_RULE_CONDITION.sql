CREATE TABLE IF NOT EXISTS config.ALLOCATION_RULE_CONDITION (
    CONDITION_ID bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    RULE_ID bigint NOT NULL REFERENCES config.ALLOCATION_RULE(RULE_ID) ON DELETE CASCADE,
    FIELD_NAME varchar(80) NOT NULL,
    OPERATOR varchar(20) NOT NULL,
    VALUE_TEXT text,
    VALUE_NUMERIC numeric(18,6),
    VALUE_BOOLEAN boolean,
    CONDITION_GROUP integer NOT NULL DEFAULT 1,
    SEQUENCE integer NOT NULL DEFAULT 100,
    NEGATE boolean NOT NULL DEFAULT false
);
