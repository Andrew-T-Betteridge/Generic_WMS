# DYNETIC WMS 0.3.11 release manifest
# Upgrade from the validated v0.3.10 production baseline.
#
# This migration contains:
# - delivery-zone basket and per-class fulfilment configuration;
# - postcode-distance calculation;
# - richer delivery options and checkout quoting;
# - mixed STANDARD/LIVESTOCK fulfilment persistence;
# - staged Code-Point refresh/audit structures;
# - DYNETIC WMS 0.3.11 system-version registration.
#
# The canonical API function definitions are embedded in migration 0023,
# so they must not also be listed separately here.
#
# Tests, fixtures, smoke/load scripts are deliberately absent.

database/migrations/0023_dynetic_wms_0_3_11.sql